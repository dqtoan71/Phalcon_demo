<?php
if ($this->session->has('user_id')) {
    echo "Welcome: ".$user_id = $this->session->get('user_id');
    echo $this->tag->linkTo(
        "index/logout",
        ", logout"
    );
}
?>
<h1><?php echo $this->flashSession->output() ?></h1>
<?php
echo $this->tag->linkTo(
    "",
    "Home"
);?>
